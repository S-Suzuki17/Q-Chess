# 環境整理の現在地
2026-09-14: 共通指示、Antigravityの入口、共有スキル2つ、検証コマンドを追加。
作業コピーの移動・コード統合・本番公開は行っていない。

## 当面の開発入口
C:/Users/souta/Documents/Codex/2026-09-09/blender-x20/work/q-gambit-app
CodexとAntigravityで、この同じリポジトリを開く。並行編集時のみ別worktreeを使う。

## 旧コピー
C:/Users/souta/.gemini/antigravity/scratch/q-gambit
旧コピーには固有の未コミット変更がある。削除・上書き・自動同期はしない。
移転前に両方のGit差分、未追跡ファイル、公開先、署名資産を照合し、変更を保全する。
日付に依存しない固定パスへの移転は、この差分統合後に行う。

## 修正スクリプト
各コピーにGit管理中のfix/patchスクリプト136本がある。
再利用可能性・参照元・適用済みかを確認してからscriptsへ移すか退役する。
今後の一回限りの作業はscratch/へ。既存の履歴を一括削除しない。

## 有効スキル
このプロジェクト独自のスキルは2つだけ。
Vercel等のプラグインはツール接続と連動するため、キャッシュを削除せず、利用実績に基づき製品設定で整理する。
